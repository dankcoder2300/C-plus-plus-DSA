#include <bits/stdc++.h>
using namespace std;

int main()
{
    // using while loop
    /*
    int n,sum=0,i=0;
    cout<<"Enter the number you want sum of: "<<endl;
    cin>>n;
    while(i<=n)
    {
        sum=sum+i;
        i++;
    }
    cout<<sum;
    */

    // using for loop
    /*
    int n,i,sum=0;
    cout<<"Enter the number you want sum of: "<<endl;
    cin>>n;
    for(int i=0;i<=n;i++)
    {
        sum=sum+i;

    }
    cout<<sum;
    */

    // using do while loop
    /*
    int n, i = 0, sum = 0;
    cout << "Enter the number you want sum of: " << endl;
    cin >> n;
    do
    {
        sum = sum + i;
        i++;
    } while (i <= n);
    cout << sum;
*/
    return 0;
}